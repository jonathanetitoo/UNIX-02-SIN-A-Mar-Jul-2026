$cd /dev #Change directory to /dev directory, but 
$ls #List directories on the /dev directory 
$history #To view all the commands I run in the terminal 
cd /proc #To change to /proc
/proc $cat cpuinfo #Views specsof the virtual cpu