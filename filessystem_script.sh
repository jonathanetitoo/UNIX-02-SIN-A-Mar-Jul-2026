#Changes the current directory to /, which is the top-level directory in Linux/Unix systems.
cd / key 
#list files and directories on the / dev directory 
ls 
#To see the history of commands you have executed in the terminal.
history
#Used to change the current directory to /proc, a special virtual filesystem in Linux/Unix.
cd /proc
ls
#Used to view detailed information about the CPU
cat cpuinfo
#Changes the current directory to /workspaces/UNIX-02-SIN-A-Mar-Jul-2026/
/proc $ cd /workspaces/UNIX-02-SIN-A-Mar-Jul-2026/
#Changes the current directory to the home directory of the logged-in user.
 cd 
#Executes a custom command or script named saludobin. In your context, this is probably the “greeting” script that prints a message like “Hello, welcome!”.
saludobin
##Changes the current directory to /etc/gss. This is likely a system configuration directory related to GSS (Generic Security Services) or other service configuration files.
cd /etc/gss
#Shows the list of previously executed commands in the current shell session. Useful to review what has been done or to reuse commands with !number.
history
#Print Working Directory, see to the full path of the current directory
pwd 
#Add symbols to quickly identify what type of file it is.
ls -F
#Displays the inode numbers of each file or directory along with its name.
ls -i